. test turn command stop condition
 . should not be a difference
. set w to something negative
. should have the starting angle
 . should have the ammount to rotate? 

. chipmunk physics engine
 . bodies
 . arbitrer, collision

. since 2d, associate a weight in case of a multiobject
 . the object color (yellow) is dependent on the weight

. scale_coords(world(x, y), x::width, x::height);
 . scale

. state snapshot
 . snap::load, snap::store
